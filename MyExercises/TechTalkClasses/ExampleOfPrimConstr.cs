namespace MyExercises;

public class Animal(string name, int age) // chose another name because you can't have 2 classes with the same name
{
    public string Name { get; } = name;
    public int Age { get; } = age;
}